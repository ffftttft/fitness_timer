import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const background = Color(0xFF121212);
  static const surface = Color(0xFF1A1A1A);
  static const surface2 = Color(0xFF222222);

  // Phase colors: warm-up (yellow) / work (orange) / rest (blue)
  static const warmup = Color(0xFFF2C94C);
  static const work = Color(0xFFFF7A18);
  static const rest = Color(0xFF2D9CDB);

  static const textPrimary = Color(0xFFEDEDED);
  static const textSecondary = Color(0xFFBDBDBD);
}

